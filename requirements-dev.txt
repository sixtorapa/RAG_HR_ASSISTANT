# Development and evaluation dependencies.
#
# Deliberately kept out of requirements-prod.txt: that list builds the image, and
# on Lambda image size is cold-start time. ragas pulls in datasets and pyarrow,
# which have no business in the container that answers requests.
#
#   pip install -r requirements-prod.txt -r requirements-dev.txt

pytest==9.1.1

# LLM judge for the evaluation suite (evaluation/*.py).
# Pinned below 0.3 for the same reason langchain-aws is pinned: 0.2.x depends on
# langchain-core 0.3.x, the line qa_chain.py lives on. 0.3 would take core to 1.x
# and the repository would stop importing.
ragas>=0.2.0,<0.3.0
