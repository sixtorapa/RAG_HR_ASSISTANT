# Dependencias de desarrollo y evaluación.
#
# NO van en requirements-prod.txt a propósito: esa lista construye la imagen, y
# en Lambda el tamaño de la imagen es tiempo de arranque en frío. RAGAS arrastra
# datasets y pyarrow, que no pintan nada en el contenedor que responde.
#
#   pip install -r requirements-prod.txt -r requirements-dev.txt

pytest==9.1.1

# Juez LLM de la suite de evaluación (evaluation/*.py).
# Pineado <0.3: la 0.2.x depende de langchain-core 0.3.x, que es la línea sobre
# la que vive qa_chain.py. La 0.3 subiría core a 1.x y el repo deja de importar,
# igual que pasó con langchain-aws.
ragas>=0.2.0,<0.3.0
